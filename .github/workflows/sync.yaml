name: Sync articles

on:
  schedule:
    - cron: '0 7 * * *' 
  workflow_dispatch:  

jobs:

  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du dépôt
        uses: actions/checkout@v4

      - name: Installer dépendances (exemple Python)
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Générer config.yaml à partir des variables d'environnement
        run: |      
          echo "usr: \"${{ secrets.USR }}\"" > config/credentials.yaml
          echo "url: \"${{ secrets.URL }}\"" >> config/credentials.yaml
          echo "pwd: \"${{ secrets.PWD }}\"" >> config/credentials.yaml

      - name: Exécuter le script
        run: |
          python sync-ddgs.py
          python sync-rss.py
